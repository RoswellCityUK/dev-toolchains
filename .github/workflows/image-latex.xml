
name: Build & Publish toolchain-latex

on:
  push:
    branches: [ "main" ]
    paths:
      - "images/latex/**"
      - ".github/workflows/image-latex.yml"
      - "Makefile"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version
        id: ver
        run: |
          echo "version=$(cat images/latex/VERSION)" >> $GITHUB_OUTPUT

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: images/latex
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/toolchain-latex:texlive-2024.${{ steps.ver.outputs.version }}
            ghcr.io/${{ github.repository_owner }}/toolchain-latex:texlive-2024
            ghcr.io/${{ github.repository_owner }}/toolchain-latex:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
